/* export default {
  generate(context) {
    const keys = Object.keys(this);
    keys.forEach((key) => {
      if (
        key !== 'generate'
      ) this[key].generate(context);
    });
  },
}; */
